# ???????????VivHelper / ?? Everest Mod ???

> ????????2026-07-31???????? Refill / FallingBlock ???
> ??? `celeste-physics` ??? Everest ???? VivHelper??????????

## 1. ???????????

???????????????? 6 ????????"????"????????

| ??? | ?? | ?? |
| --- | --- | --- |
| ??/?? | `crates/celeste-physics/src/map.rs` | `.bin` ??? ? `EntityKind`?bounds/direction ??? |
| ???? | `crates/celeste-physics/src/types.rs` | `PlayerSnapshot` ?? `Vec<XxxSnapshot>`?????????? |
| ??? | `sim.rs` `initialize_*` | ? map ???????? |
| ???? | `sim.rs` `advance_*` | ????/??/?? |
| ???? | `sim.rs` `interact()` | PlayerCollider ??????? hurtbox ??? |
| ???? | `map.rs` `non_dream_solid_at` / `sim.rs` `solid_collision_env` / `runtime_solid_collision` | ???? Solid/JumpThru ?????? |
| ?? | `web/src/components/GameView.tsx` | ??/???? |
| ?? E2E | `scripts/e2e-real/scenarios/playground/*.ts` | Everest ???? + Rust ?? `<= 0.01` |

?????VivHelper ??????"????????? + C# ??"???????
`.bin` ? entities ????? `EntityKind::Unknown` + `name` ?????????
?`map.rs` ??? `name: el.name.clone()` ???????????????????
?????

## 2. ????

1. **?????????**???????? + ??? match ??????????????
2. **????????**?`Unknown` ????? no-op???? `name` ???????
3. **?????**??????????????msgpack????????????
4. **?? E2E ????**???"???"????????? Everest ?? `<= 0.01`?

## 3. ?????????? + ?????

### 3.1 Rust ??`ModEntityBehavior` trait???????

```rust
/// ??????????WASM ????????????? crate/feature ??
/// ??????? Everest ????? .dll ???????????????????
pub trait ModEntityBehavior: Send + Sync {
    fn name(&self) -> &'static str;              // ??????? "VivHelper/CustomBooster"
    fn collider(&self, state: &ModEntityState) -> Option<Rect>; // ?? Solid/JumpThru ??
    fn advance(&self, ctx: &mut SimContext, state: &mut ModEntityState, dt: f32);
    fn interact(&self, ctx: &mut SimContext, state: &mut ModEntityState, player: &mut PlayerSnapshot);
    fn snapshot(&self, state: &ModEntityState) -> rmp_serde::encode::Value; // ??? msgpack
    fn restore(&self, blob: rmp_serde::decode::Value) -> ModEntityState;
}
```

- `SimContext` ????/??????? LiftSpeed?Freeze????"??? match ???
  ????"???????????????
- `ModEntityState` ??? `Vec<u8>`/`serde_json::Value` ????????????
  `PlayerSnapshot` ?? `mod_entities: Vec<ModEntitySnapshot>`?
  `ModEntitySnapshot { name: String, state: Value }`????? fork/??????

### 3.2 ????

- ?? crate ??? trait ???? `std::sync::OnceLock<Vec<Box<dyn ModEntityBehavior>>>`?
- ??????? crate?? `crates/celeste-physics-mods-vivhelper`??
  ? `lib.rs` ? `register_mod_entity!(VivHelperBooster);` ????
  ? crate ?? feature?? `--features mod-vivhelper`????
- ??? Everest ?"?? = ?? dll??????"???????????WASM ????

### 3.3 ??????????

- `map.rs` ???`_ => EntityKind::Unknown` ?????? Unknown ????
  `name`????+ `nodes`????????`Unknown` ??? `encode` ???
  ??? `name` ??????? `EntityKind::Unknown => None` ????????
  "???????????? ? ???????? ? ?????"??
- `map_fixture.rs`?fixture ??? `name: Option<String>`???????????
  ??? `.bin` ??????

### 3.4 ?? E2E ??Collector ???????

- ?? collector ?????????????????"?????"??????
  ? `SnapshotCapture` ??? `IModEntitySnapshotProvider` ???
  VivHelper ???? Everest ????????? collector????? Mod ??????
  ???? `{ name, state }` msgpack?
- Rust ???`compare_real_trace`???? `restore()` ????????
  ???????????"???????"????????????
  `bumpers`/`spinners` ?????????`initialize_bumpers` ?
  "captured state supplies only its live Position and phase"??

## 4. ??????????????????

?? trait ?????? VivHelper ??"?????????"??????????

1. **????????????**????????"???"?????????????
   - ??? Booster ? `BoosterBehavior { radius, launch_speed, path_nodes }`?
     ?? `boost_target`/`boost_red` ????
   - ????? ? ?? `MovingSolid`????"???????? Solid"????/???
   - ?????/?/?? ? ???? `Spikes`/`Cloud`/`Spring` ? direction ???
   - ?????? `EntityKind` ?????????????????"????"?
     ?????VivHelper ???????booster ???spinner ???????????
2. **?????????**????"???????"????????/????????
   ???? 3 ?? `ModEntityBehavior` trait?

## 5. ?????VivHelper ???

- [ ] `map.rs`?Unknown ???? name/nodes ???"????"?
- [ ] `map_fixture.rs`??? fixture ?? `name` ? Unknown ???
- [ ] `types.rs`?`mod_entities: Vec<ModEntitySnapshot>`???? msgpack??
- [ ] `sim.rs`???? + `advance_mod_entities` + `interact` ? Unknown ???
- [ ] `web/GameView.tsx`?Unknown ??? `drawUnknownEntity`??????????
- [ ] Collector?`IModEntitySnapshotProvider` ?? + ?????
- [ ] ?????VivHelper ??? booster ????? 4 ???? + ?? E2E `<= 0.01`?

## 6. ????

- ???????????"???"???????????? `Unknown`?no-op??
  ?????????tech handbook????? verdict??
  `INTENTIONALLY_UNSUPPORTED_STATES`/FinalBoss ????????
- ?? C# ???? Rust??????????????????????
  ???"???"?????

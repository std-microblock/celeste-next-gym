# Technique media

Store handbook preview images in this directory. Prefer a 16:9 WebP or PNG at least 1280 px wide. Animated GIFs and videos need a separate static preview because the PDF output cannot play them; link the original media through `media-item(url: ...)`.
